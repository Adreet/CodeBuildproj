version: 0.2

phases:
  pre_build:
    commands:
        # install npm
        - npm install     
  build:
    commands:
        - npm run build
     
artifacts:
  files:
    -"**/*"
  discard-paths: yes
  base-directory: build
  
